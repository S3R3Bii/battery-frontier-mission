# Container Notes

The root `Dockerfile` runs the Streamlit wireframe. Production deployment must
add authentication, TLS termination, immutable data volumes, backups, and a
review gate before public report publication.

